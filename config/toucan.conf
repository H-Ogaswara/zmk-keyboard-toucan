# --- Power saving: idle/sleep ---
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_TIMEOUT=60000            # 1分でアイドル
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=300000     # 5分でディープスリープ（旧: ZMK_SLEEP_TIMEOUT）

# --- BLE power saving ---
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=24
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=40
CONFIG_BT_PERIPHERAL_PREF_LATENCY=4      # 旧: BT_PERIPHERAL_PREF_SLAVE_LATENCY
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=500
